---
import MaterialIcon from '@/components/Base/MaterialIcon.astro';
import '@/styles/animations/return-arrow.css';
---

<nav>
  <a href="/">
    <MaterialIcon name="keyboard_backspace" class="icon" />
  </a>
</nav>

<style>
  nav {
    position: relative;
    padding: 1rem var(--FEATURED_CARD_MARGIN);
  }

  nav a {
    display: inline-flex;
    text-decoration: none;
  }

  nav a:hover,
  nav a:focus-visible {
    animation: return-arrow 0.6s infinite;
  }

  nav a .icon {
    color: rgb(var(--TEXT_COLOR));
    font-size: 3rem;
  }
</style>
